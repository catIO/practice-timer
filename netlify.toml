[build]
  base = "."
  publish = "client/dist"
  command = "npm run build"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "20.12.2"

[[redirects]]
  from = "/api/metadata"
  to = "/.netlify/functions/metadata"
  status = 200
  force = true

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
  force = true

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[[headers]]
  for = "/*.js"
  [headers.values]
    Content-Type = "application/javascript"

[[headers]]
  for = "/*.mjs"
  [headers.values]
    Content-Type = "application/javascript"

# Don't cache SW or HTML so users get latest app after deploy (SW and app use their own caching)
[[headers]]
  for = "/sw.js"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"

[[headers]]
  for = "/index.html"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"

[dev]
  base = "."
  framework = "#custom"
  command = "npm run dev"
  publish = "client/dist"
  port = 3000
  targetPort = 5173
